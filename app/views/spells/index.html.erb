<!DOCTYPE html>
<html>
  <head>

    <title>✨HexCode✨</title>
    <link rel="stylesheet" media="all" href="/assets/spells-index.scss" data-turbolins-tracks="reload">

  </head>
  <body>

    <nav>
        <%= link_to "Profile", @current_user, class: "nav-link" %>
        <%= link_to "Create a spell", new_spell_path, class: "nav-link" %>
        <%= form_tag "/spells", method: :get%>
          <%= text_field_tag :search%>
        <%= submit_tag "Search" %>
    </nav>

    <center><h1> Your Spellbook </h1></center>
      <div class="spells-index">
          <%@spells.each do |spell| %>
            <div class="spell-card">
                <%= link_to spell.name, spell, class:'link-to-text-color'%>
              </div>
            <%end %>
        </div>


</body>
